let names = ["John", "Jane", "Jim", "Jill"];
let values = [true, {}, [], 2, "hello"];

export { names, values };

/**
 * insertion: depends
 * removal: depends
 * searching: O(n)
 * access: O(1)
 */

/**
 * push: O(1)
 * pop: O(1)
 * shift: O(n)
 * unshift: O(n)
 * concat: O(n)
 * slice: O(n)
 * splice: O(n)
 * sort: O(n log n)
 * forEach/map/filter/reduce: O(n)
 * reduce: O(n)
 */

